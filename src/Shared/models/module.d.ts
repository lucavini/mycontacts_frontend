export as namespace models;

export interface Contact {
  category_id: string;
  category_name: string;
  email: string;
  id: string;
  name: string;
  phone: string;
}

export interface Category {
  category_id: string;
  category_name: string;
  email: string;
  id: string;
  name: string;
  phone: string;
}
